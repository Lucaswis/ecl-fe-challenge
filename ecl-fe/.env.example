# Base URL of the upstream asset backend (server-only; never exposed to the client).
# Read via getBackendUrl() in src/lib/assets/backend.ts, which falls back to
# http://localhost:8080 when this is unset.
BACKEND_URL=http://localhost:8080
